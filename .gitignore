__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.hypothesis/
.tox/
.nox/
.DS_Store
.idea/
.vscode/
.venv/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Python packaging and local build outputs.
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/

# Secrets and machine-specific configuration.  Keep a checked-in .env.example
# if one is added later, but never commit a usable credential file.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json

# Supplied catalog/evaluation data and generated local indexes are not part of
# the submission bundle.  Keep data/README.md as the acquisition guide.
data/catalog.jsonl
data/catalog.jsonl.gz
data/public_set.jsonl
data/releases/
*.sqlite
*.sqlite3
*.db

# Local evaluator output and experiment artifacts are reproducible and must
# not be submitted as source.
results*.json
*.prof
*.log

# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
# Local planning document; do not include in the public submission repository.
PRD_TRACK4_SHOPPING_COPILOT.md
